json : rf_classification.json
# Name of input data

input_data : mfcc_RAW.json

# Choose the algorithm for the second dim reduction
# Choices are: Naive Bayes (NB), Logistic Regression (LR), State Vector Machine (SVM) or MLP Neural Network (MLP)
algorithm : RF

# Choose the kind of data normalisation you want before it goes into the classifer
# Options are minmax or meanstddev or flat none
normalisation : minmax

# Do you want to hear playback?
playback : false

feedback : true