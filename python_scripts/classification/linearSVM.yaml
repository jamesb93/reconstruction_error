json : lin_svm_classification.json
# Name of input data

input_data : mfcc_RAW.json

# Choose the algorithm for the second dim reduction
# Choices are: 
# Naive Bayes (NB) 
# Logistic Regression (LR)
# State Vector Machine (SVM)
# MLP Neural Network (MLP)
# linearSVM (linSVC)
# random forest (RF)
algorithm : linSVC

# Choose the kind of data normalisation you want before it goes into the classifer
# Options are minmax or meanstddev
normalisation : standardise